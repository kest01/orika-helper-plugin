#if (${PACKAGE_NAME} && $PACKAGE_NAME != "" )package ${PACKAGE_NAME}
#end
${IMPORTS}

/**
 * Test for Orika mapping for classes ${SOURCE_CLASS} and ${DEST_CLASS}
 */
class ${TEST_CLASSNAME} {

    def mapper = new $MAPPER_CLASS()

    ${TEST_METHOD}
}